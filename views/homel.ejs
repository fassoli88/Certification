  <!doctype html>
  <html lang="en">

  <head>
    <script src="/javascript/home.js"></script>
    <script crossorigin src="https://unpkg.com/react@18/umd/react.development.js"></script>
    <script crossorigin src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>

    <title>Certification</title>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="stylesheet" href="/css/homel.css">
    <!-- Bootstrap CSS v5.2.1 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" rel="stylesheet"
      integrity="sha384-iYQeCzEYFbKjA/T2uDLTpkwGzCiq6soy8tYaI1GyVh/UjpbCx/TYkiZhlZB6+fzT" crossorigin="anonymous">

  </head>

  <body>
  <div id="app">

  </div>

  <script type="text/babel">
    function App(){

      const [state, setState]= React.useState({
        names: [],
        newname: "",
        listname: "",
        

      });
      
      function inputKey(event){
            setState({
              ...state,
              names: [...state.names, `${state.newname}`], 
            });          
      }


      function ListName(event){  
          setState(
            listname= state.names.map((name, index)=>
            <li key={index}>
              {name}
              </li>)   
            )
      }

      function updaten(event){
        setState({
          ...state,
          newname: event.target.value,
        });
      }
      
      function Hello(){
        return(
          <div>
            list
          </div>
        );
      }
      function Clear(){
        setState({
          ...state, 
          names: [] 
        });
      }
      return(
        <div style={{textAlign: "center"}}>
        <h1>My list</h1>  
        <ul>
          
        </ul>
        <input onChange={updaten} value={state.newname.value} />           
        <br/>
        <br/>
        <button onClick={inputKey}> Add to List </button>
        <button onClick={ListName}>show me</button>
        <button onClick={Clear}> Delete </button>
        </div>
      );
    }

    ReactDOM.render(<App/>, document.querySelector("#app"));
    //setTimeout(function(){ window.location.reload(); }, 5000);
    
          
  </script>
  
  <!-- Bootstrap JavaScript Libraries -->
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"
    integrity="sha384-oBqDVmMz9ATKxIep9tiCxS/Z9fNfEXiDAYTujMAeBAsjFuCZSmKbSSUnQlmh/jp3" crossorigin="anonymous">
  </script>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/js/bootstrap.min.js"
    integrity="sha384-7VPbUDkoPSGFnVtYi0QogXtr74QeVeeIs99Qfg5YCF+TidwNdjvaKZX19NZ/e6oz" crossorigin="anonymous">
  </script>
</body>

</html>